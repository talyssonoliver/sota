[tool:pytest]
# Minimal safe configuration
testpaths = tests
python_files = test_*.py
addopts = --tb=short --disable-warnings --maxfail=3 -x --timeout=30
markers =
    unit: Unit tests
    integration: Integration tests
env =
    TESTING=1
    PYTHONDONTWRITEBYTECODE=1
